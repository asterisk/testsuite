[general]
static=yes
writeprotect=yes
autofallthrough=yes
clearglobalvars=no
priorityjumping=yes

[globals]

[default]
exten => 555,1,noOp()
    same => n,Answer()
	same => n,SendDTMF(1)
    same => n,SendDTMF(2)
    same => n,SendDTMF(3)
    same => n,Hangup()
