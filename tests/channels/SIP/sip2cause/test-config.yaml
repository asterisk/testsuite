testinfo:
    summary: 'Test SIP failure code to internal cause code conversion (hangup_sip2cause).'
    description: |
        'Test whether certain SIP response codes (404) are mapped to
        the Q.850 equivalent (1/UNALLOCATED).'

properties:
    dependencies:
        - python: 'starpy'
        - sipp:
            version: 'v3.1'
        - asterisk : 'chan_sip'
    tags:
        - SIP
        - sip_cause
