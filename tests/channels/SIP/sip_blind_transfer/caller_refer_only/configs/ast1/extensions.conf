[general]

[globals]

[transfertest]
exten => call_b,1,NoOp()
	same => n,Dial(SIP/end_b)
	same => n,Hangup()

exten => call_c,1,NoOp()
	same => n,Dial(SIP/end_c)
	same => n,Hangup()

exten => h,1,NoOp()