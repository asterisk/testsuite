testinfo:
    summary:     'Test reception of SIP REGISTER requests using TLS'
    description: |
        'A UAC registers with Asterisk.  It uses TLS with IPv4 addresses.'

properties:
    minversion: '1.8.0.0'
    dependencies:
        - python : 'twisted'
        - python : 'starpy'
        - sipp :
            version : 'v3.0'
        - asterisk : 'chan_sip'
    tags:
        - SIP
