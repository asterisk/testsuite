
[default]

exten => app_exec,1,NoOp()
	same => n,Answer()
	same => n,Echo()
	same => n,Hangup()

exten => dial_foo,1,NoOp()
	same => n,Dial(SIP/foo@asterisk)
	same => n,Hangup()

exten => foo,1,NoOp()
	same => n,Answer()
	same => n,Playback(beep)
	same => n,Hangup()
