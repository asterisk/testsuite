[transport-udp]
type = transport
protocol = udp
bind = 127.0.0.1

[asterisk]
type = aor
contact = sip:127.0.0.1

[asterisk]
type = endpoint
allow = ulaw
aors = asterisk

[asterisk]
type = identify
endpoint = asterisk
match = 127.0.0.1

