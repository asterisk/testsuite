[default]

exten = alice,1,Dial(Local/bob@default)
 same = n,Hangup()

exten = bob,1,Answer()
 same = n,AMD()
 same = n,Playback(silence/1&goodbye&silence/1)
 same = n,Hangup()

exten = bridgeadd,1,BridgeAdd(Local/alice@default-)
 same = n,Hangup(50)

exten = charlie,1,Answer()
 same = n,Playback(silence/1&hello&silence/1)
 same = n,AMD()
 same = n,Hangup()

