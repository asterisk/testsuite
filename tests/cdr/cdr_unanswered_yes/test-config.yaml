testinfo:
    summary: 'Test the unanswered=yes cdr.conf option'
    description: |
        'Do a simple unanswered call and check for a CDR'

properties:
    minversion: '1.8.0.0'
    dependencies:
        - python : 'twisted'
        - python : 'starpy'
        - asterisk : 'cdr_csv'
    tags:
        - CDR
        - chan_local
