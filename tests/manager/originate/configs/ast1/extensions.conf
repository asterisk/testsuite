[default]

exten => one,1,NoOp()
    same => n,Answer()
	same => n,Wait(1)
    same => n,Hangup()

exten => two,1,NoOp()
    same => n,Answer()
	same => n,Wait(1)
    same => n,Hangup()

exten => three,1,NoOp()
    same => n,Answer()
	same => n,Wait(1)
    same => n,Hangup()
