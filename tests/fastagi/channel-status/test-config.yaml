testinfo:
    summary:     'Test AGI CHANNEL STATUS command using FastAGI.'
    description: |
        "Test AGI CHANNEL STATUS command using FastAGI. Note: This test also 
        relies on the ANSWER command, so this test can be considered 
        a test for both commands."


properties:
    minversion: '1.4'
    dependencies:
        - app : 'python'
        - python : 'twisted'
        - python : 'starpy'
        - asterisk : 'app_talkdetect'
