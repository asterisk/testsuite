testinfo:
    summary:     'Test AGI HANGUP command using FastAGI.'
    description: |
        'Test AGI HANGUP command using FastAGI. Note: this test requires that
        CHANNEL STATUS throw an error when it is asked to check the status
        of a dead channel.'


properties:
    minversion: '1.4'
    dependencies:
        - app : 'python'
        - python : 'twisted'
        - python : 'starpy'
    tags:
        - AGI