testinfo:
    summary:     'Test PickupChan'
    description: |
        'This test verifies functionality of the PickupChan app and its options'

properties:
    minversion: '1.8.0.0'
    dependencies:
        - python : 'twisted'
        - python : 'starpy'
    tags:
        - iax2
        - pickup
