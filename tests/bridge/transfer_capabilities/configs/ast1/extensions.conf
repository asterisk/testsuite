[default]

exten => alice_opt,1,Dial(SIP/test_call@bob,,Th)
same => n,Hangup()

exten => alice_bridge_features,1,Set(BRIDGE_FEATURES=T)
same => n,Dial(SIP/test_call@bob,,h)
same => n,Hangup()

exten => bob_opt,1,Dial(SIP/test_call@bob,,tH)
same => n,Hangup()

exten => bob_bridge_features,1,Set(BRIDGE_FEATURES=t)
same => n,Dial(SIP/test_call@bob,,H)
same => n,Hangup()

