[default]

exten => no_warning,1,Dial(SIP/test_call@bob,,L(4000))
same => n,Hangup()

exten => caller_warning,1,Dial(SIP/test_call@bob,,L(10000:5000))
same => n,Hangup()

exten => callee_warning,1,Set(LIMIT_PLAYAUDIO_CALLER=no)
same => n,Set(LIMIT_PLAYAUDIO_CALLEE=yes)
same => n,Dial(SIP/test_call@bob,,L(10000:5000))

exten => both_warning,1,Set(LIMIT_PLAYAUDIO_CALLEE=no)
same => n,Set(LIMIT_PLAYAUDIO_CALLER=yes)
same => n,Dial(SIP/test_call@bob,,L(10000:5000))
