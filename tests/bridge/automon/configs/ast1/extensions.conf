[default]

exten => alice_automon,1,Dial(SIP/test_call@bob,,Wh)
same => n,Hangup

exten => bob_automon,1,Dial(SIP/test_call@bob,,wH)
same => n,Hangup

exten => alice_automon2,1,Set(BRIDGE_FEATURES=W)
same => n,Dial(SIP/test_call@bob,,h)
same => n,Hangup

exten => bob_automon2,1,Set(BRIDGE_FEATURES=w)
same => n,Dial(SIP/test_call@bob,,H)
same => n,Hangup
