testinfo:
    summary: 'Ensure that the channel format can be changed.'
    description: |
        'This ensures that the channel format can be changed in the appropriate place.'

test-modules:
    test-object:
        config-section: test-object-config
        typename: 'test_case.TestCaseModule'
    modules:
        -
            config-section: caller-originator
            typename: 'pluggable_modules.Originator'
        -
            config-section: hangup-monitor
            typename: 'pluggable_modules.HangupMonitor'
        -
            config-section: ami-config
            typename: 'pluggable_modules.EventActionModule'

test-object-config:
    connect-ami: True

caller-originator:
    channel: 'Local/s@default'
    context: 'nothing'
    exten: 's'
    priority: '1'
    trigger: 'ami_connect'

hangup-monitor:
    ids: '0'

ami-config:
    -
        ami-events:
            conditions:
                match:
                    Event: 'UserEvent'
                    UserEvent: 'ReceiveMFSuccess'
            requirements:
                match:
                    Result: 'Pass'
            count: 1
        stop_test:

properties:
    tags:
        - dial
        - apps
    dependencies:
        - python: 'twisted'
        - python: 'starpy'
        - asterisk: 'app_dial'
        - asterisk: 'app_userevent'
        - asterisk: 'app_originate'
        - asterisk: 'app_mf'
        - asterisk: 'chan_iax2'
        - asterisk: 'pbx_config'
