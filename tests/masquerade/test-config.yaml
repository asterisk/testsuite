testinfo:
    summary:     'Test Masquerading'
    description: |
        'Create a long chain of local channels to be optimized out.'

properties:
    minversion: '1.8.0.0'
    dependencies:
        - python : 'twisted'
        - python : 'starpy'
    testconditions:
        - name: 'threads'
          ignoredThreads:
            - 'autoservice_run'
    tags:
        - parking
        - iax2
