[default]

exten => s,1,NoOp()
	same => n,Answer()
	same => n,Stasis(testsuite)
	same => n,Hangup()

exten => second,1,NoOp()
	same => n,Answer()
	same => n,Stasis(testsuite,second)
	same => n,Hangup()

exten => echo,1,NoOp()
	same => n,Echo()
