[default]

exten => s,1,NoOp()
 same => n,Answer()
 same => n,Stasis(testsuite)
 same => n,Hangup()

[send-message-local]

exten => s,1,NoOp()
 same => n,Answer()
 same => n,Stasis(testsuite)
 same => n,Hangup()

[send-message-nonstasis]

exten => s,1,NoOp()
 same => n,Answer()
 same => n,Wait(30)
 same => n,Hangup()
