testinfo:
    summary:     'Test Call Pickup'
    description: |
        'This test verifies that a channel can use the call pickup extension to
        Answer a call to a channel that is assigned to its pickupgroup.'

properties:
    dependencies:
        - python : 'twisted'
        - python : 'starpy'
        - asterisk : 'chan_pjsip'
    tags:
        - pickup
        - features
